{
  "name": "mazyos-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd patricia-julio && npm install && npm run build",
    "dev": "cd patricia-julio && npm run dev",
    "start": "cd patricia-julio && npm start",
    "install": "cd patricia-julio && npm install && npx playwright install chromium"
  }
}
